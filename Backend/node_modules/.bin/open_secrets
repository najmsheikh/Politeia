#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../open_secrets/open_secrets.js" "$@"
  ret=$?
else 
  node  "$basedir/../open_secrets/open_secrets.js" "$@"
  ret=$?
fi
exit $ret
